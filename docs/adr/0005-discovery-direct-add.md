# Discovery by direct-add, seed list scaffolded

V1 discovery is paste-a-URL direct-add fetching the sidecar card, plus a defined list-file format that ships scaffolded (empty plus example). No crawler, ranking, or hosted search in v1. A mall later reads the same format with zero money-core changes.
