# The demo, as five services (§10). Mount paths and the origin contract are the
# product; the public demo runs the same split on Fly.io as three apps, because
# Fly scopes secrets per app rather than per process (ADR-0019).
#
# `store` and `buyer-chat` are built by their own phases (B1, C1). Until then
# Caddy's routes for them answer 502, which is the honest answer — the route
# exists, the upstream does not yet.

name: openstore

services:
  caddy:
    image: caddy:2.8-alpine
    ports:
      - "80:80"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
    depends_on:
      sidecar:
        condition: service_healthy
    networks:
      default:
        aliases:
          # `*.localhost` resolves to the container's OWN loopback inside a
          # container, not to the edge (§10.1). A service that fetches the
          # demo's public URL — the chat fetching a shop card — needs the name
          # to mean Caddy here, or it dials itself and gets nothing.
          - spoiledduckie.localhost
          - dogeared.localhost
          - circuityard.localhost
          - ironlist.localhost
          - pantryline.localhost
          - kettleandgrain.localhost
          - deskfield.localhost
          - rootandleaf.localhost
          - playspool.localhost
          - furrow.localhost
          - chat.localhost
    restart: unless-stopped

  sidecar:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      # One deploy, one Merchant domain (ADR-0007).
      OPENSTORE_MERCHANT_DOMAIN: spoiledduckie.localhost
      OPENSTORE_PUBLIC_ORIGIN: http://spoiledduckie.localhost
      WEBAUTHN_RP_ID: spoiledduckie.localhost
      WEBAUTHN_RP_NAME: SpoiledDuckie
      SIDECAR_DATABASE_URL: postgresql+psycopg://sc_app:sc_app_dev@postgres:5432/sidecar
      STORE_INTERNAL_URL: http://store:3000
      # The ORIGIN, not the /trait prefix: the client appends `/trait/<door>`
      # itself, and doubling it produces /trait/trait/catalog.read.
      TRAIT_BASE_URL: http://store:3000
      # Both sides of the trait need the same secret. Setting it on the store
      # alone gives the sidecar nothing to sign with, and every door refuses.
      TRAIT_HMAC_SECRET: conformance-secret
      PAYMENT_PROVIDER: fake
      # Pseudonymous consumer ids (ADR-0011). Demo value; a real deploy
      # generates one and rotating it unlinks old orders from new.
      DEPLOY_PSEUDONYM_KEY: demo-deploy-pseudonym-key-32bytes
      # The second admission route (ADR-0012). Self-registration needs no
      # Merchant action; these are for an agent the Merchant has issued
      # credentials to, and without them that route refuses everyone.
      OAUTH_CLIENT_ID: demo-agent
      OAUTH_CLIENT_SECRET: demo-agent-secret
      # The Merchant's signing keys, on a volume because they ARE the Merchant's
      # identity: a key regenerated on each boot breaks every agent that pinned
      # the last one and orphans every receipt already sealed (ADR-0014).
      SIDECAR_SIGNING_KEY_PATH: /keys/signing-keys.json
      OPENSTORE_DEMO_MODE: "true"
      # The SSRF exception, for the compose demo's own chat and nothing else
      # (§10.1). Boot is refused if this is set alongside live provider keys.
      OPENSTORE_DEV_PROFILE_HOSTS: buyer-chat:3001
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - sidecarkeys:/keys
    expose:
      - "8000"
    restart: unless-stopped

  store:
    build:
      context: .
      dockerfile: demo/merchant-site/Dockerfile
    environment:
      PORT: "3000"
      MERCHANT_DATABASE_URL: postgresql://sd_app:sd_app_dev@postgres:5432/spoiledduckie
      TRAIT_HMAC_SECRET: conformance-secret
      SIDECAR_INTERNAL_URL: http://sidecar:8000
      OPENSTORE_PUBLIC_ORIGIN: http://spoiledduckie.localhost
      ADMIN_SEED_PASSWORD: demo-operator-pw
      # adapter-node assumes https when no protocol header is named, so the
      # origin it computes is https://<host> while the browser posts from
      # http://<host> — and SvelteKit's CSRF check refuses every form on the
      # demo's plain-http origins. Caddy sets this header; honour it.
      PROTOCOL_HEADER: x-forwarded-proto
      NODE_ENV: production
    depends_on:
      postgres:
        condition: service_healthy
    expose:
      - "3000"
    ports:
      # Loopback only, and deliberately: the nine doors live here and the edge
      # refuses them. This is the private network, for the seed script and the
      # conformance suite. Never bind it to 0.0.0.0.
      - "127.0.0.1:3000:3000"
    restart: unless-stopped

  # The second shop, and the point of it: the same two images as above with a
  # different domain, a different database and a different catalogue. Nothing
  # is forked — `sidecar-books` is the sidecar image, `store-books` is the
  # merchant-site image, and everything that differs is an environment
  # variable. A shop that needed its own build would mean this site is one
  # shop with a schema rather than a template (ADR-0007: one deploy, one
  # Merchant domain).
  sidecar-books:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      OPENSTORE_MERCHANT_DOMAIN: dogeared.localhost
      OPENSTORE_PUBLIC_ORIGIN: http://dogeared.localhost
      WEBAUTHN_RP_ID: dogeared.localhost
      WEBAUTHN_RP_NAME: Dog-Eared
      SIDECAR_DATABASE_URL: postgresql+psycopg://scb_app:scb_app_dev@postgres:5432/sidecar_books
      STORE_INTERNAL_URL: http://store-books:3000
      TRAIT_BASE_URL: http://store-books:3000
      TRAIT_HMAC_SECRET: conformance-secret-books
      PAYMENT_PROVIDER: fake
      # Its own pseudonym key: the same Consumer must not be linkable across
      # two Merchants by a shared id (ADR-0011).
      DEPLOY_PSEUDONYM_KEY: demo-books-pseudonym-key-32bytes
      OAUTH_CLIENT_ID: demo-agent
      OAUTH_CLIENT_SECRET: demo-agent-secret-books
      # Its own keys on its own volume. Two Merchants sharing a signing key
      # would make either one able to seal the other's receipts.
      SIDECAR_SIGNING_KEY_PATH: /keys/signing-keys.json
      OPENSTORE_DEMO_MODE: "true"
      OPENSTORE_DEV_PROFILE_HOSTS: buyer-chat:3001
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - booksidecarkeys:/keys
    expose:
      - "8000"
    restart: unless-stopped

  store-books:
    build:
      context: .
      dockerfile: demo/merchant-site/Dockerfile
    environment:
      PORT: "3000"
      MERCHANT_DATABASE_URL: postgresql://de_app:de_app_dev@postgres:5432/dogeared
      TRAIT_HMAC_SECRET: conformance-secret-books
      SIDECAR_INTERNAL_URL: http://sidecar-books:8000
      OPENSTORE_PUBLIC_ORIGIN: http://dogeared.localhost
      ADMIN_SEED_PASSWORD: demo-operator-pw
      # Which shop this deploy is. Read at runtime by `src/lib/brand.ts`, so
      # the image is the one built for SpoiledDuckie.
      PUBLIC_SHOP_NAME: Dog-Eared
      PUBLIC_SHOP_TAGLINE: "Books, secondhand and new, off Bidhan Sarani."
      PUBLIC_SHOP_BLURB: a bookshop in Kolkata
      PUBLIC_SHOP_ACCENT: "#6f9a8d"
      SEED_PROFILE: dogeared
      PROTOCOL_HEADER: x-forwarded-proto
      NODE_ENV: production
    depends_on:
      postgres:
        condition: service_healthy
    expose:
      - "3000"
    ports:
      # Loopback only, like the first shop's: the nine doors live here and the
      # edge refuses them.
      - "127.0.0.1:3010:3000"
    restart: unless-stopped

  sidecar-cy:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      OPENSTORE_MERCHANT_DOMAIN: circuityard.localhost
      OPENSTORE_PUBLIC_ORIGIN: http://circuityard.localhost
      WEBAUTHN_RP_ID: circuityard.localhost
      WEBAUTHN_RP_NAME: "CircuitYard"
      SIDECAR_DATABASE_URL: postgresql+psycopg://sccy_app:sccy_app_dev@postgres:5432/sidecar_cy
      STORE_INTERNAL_URL: http://store-cy:3000
      TRAIT_BASE_URL: http://store-cy:3000
      TRAIT_HMAC_SECRET: conformance-secret-cy
      PAYMENT_PROVIDER: fake
      DEPLOY_PSEUDONYM_KEY: demo-cy-pseudonym-key-32bytes
      OAUTH_CLIENT_ID: demo-agent
      OAUTH_CLIENT_SECRET: demo-agent-secret-cy
      SIDECAR_SIGNING_KEY_PATH: /keys/signing-keys.json
      OPENSTORE_DEMO_MODE: "true"
      OPENSTORE_DEV_PROFILE_HOSTS: buyer-chat:3001
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - cysidecarkeys:/keys
    expose:
      - "8000"
    restart: unless-stopped

  store-cy:
    build:
      context: .
      dockerfile: demo/merchant-site/Dockerfile
    environment:
      PORT: "3000"
      MERCHANT_DATABASE_URL: postgresql://cy_app:cy_app_dev@postgres:5432/circuityard
      TRAIT_HMAC_SECRET: conformance-secret-cy
      SIDECAR_INTERNAL_URL: http://sidecar-cy:8000
      OPENSTORE_PUBLIC_ORIGIN: http://circuityard.localhost
      ADMIN_SEED_PASSWORD: demo-operator-pw
      PUBLIC_SHOP_NAME: "CircuitYard"
      PUBLIC_SHOP_TAGLINE: "Cables, chargers and small gadgets."
      PUBLIC_SHOP_BLURB: "an electronics counter in Pune"
      PUBLIC_SHOP_ACCENT: "#3f6b8a"
      SEED_PROFILE: circuityard
      PROTOCOL_HEADER: x-forwarded-proto
      NODE_ENV: production
    depends_on:
      postgres:
        condition: service_healthy
    expose:
      - "3000"
    ports:
      - "127.0.0.1:3020:3000"
    restart: unless-stopped

  sidecar-il:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      OPENSTORE_MERCHANT_DOMAIN: ironlist.localhost
      OPENSTORE_PUBLIC_ORIGIN: http://ironlist.localhost
      WEBAUTHN_RP_ID: ironlist.localhost
      WEBAUTHN_RP_NAME: "IronList"
      SIDECAR_DATABASE_URL: postgresql+psycopg://scil_app:scil_app_dev@postgres:5432/sidecar_il
      STORE_INTERNAL_URL: http://store-il:3000
      TRAIT_BASE_URL: http://store-il:3000
      TRAIT_HMAC_SECRET: conformance-secret-il
      PAYMENT_PROVIDER: fake
      DEPLOY_PSEUDONYM_KEY: demo-il-pseudonym-key-32bytes
      OAUTH_CLIENT_ID: demo-agent
      OAUTH_CLIENT_SECRET: demo-agent-secret-il
      SIDECAR_SIGNING_KEY_PATH: /keys/signing-keys.json
      OPENSTORE_DEMO_MODE: "true"
      OPENSTORE_DEV_PROFILE_HOSTS: buyer-chat:3001
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ilsidecarkeys:/keys
    expose:
      - "8000"
    restart: unless-stopped

  store-il:
    build:
      context: .
      dockerfile: demo/merchant-site/Dockerfile
    environment:
      PORT: "3000"
      MERCHANT_DATABASE_URL: postgresql://il_app:il_app_dev@postgres:5432/ironlist
      TRAIT_HMAC_SECRET: conformance-secret-il
      SIDECAR_INTERNAL_URL: http://sidecar-il:8000
      OPENSTORE_PUBLIC_ORIGIN: http://ironlist.localhost
      ADMIN_SEED_PASSWORD: demo-operator-pw
      PUBLIC_SHOP_NAME: "IronList"
      PUBLIC_SHOP_TAGLINE: "Hand tools and hardware, no fuss."
      PUBLIC_SHOP_BLURB: "a hardware shop in Chennai"
      PUBLIC_SHOP_ACCENT: "#7a5c3e"
      SEED_PROFILE: ironlist
      PROTOCOL_HEADER: x-forwarded-proto
      NODE_ENV: production
    depends_on:
      postgres:
        condition: service_healthy
    expose:
      - "3000"
    ports:
      - "127.0.0.1:3030:3000"
    restart: unless-stopped

  sidecar-pl:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      OPENSTORE_MERCHANT_DOMAIN: pantryline.localhost
      OPENSTORE_PUBLIC_ORIGIN: http://pantryline.localhost
      WEBAUTHN_RP_ID: pantryline.localhost
      WEBAUTHN_RP_NAME: "PantryLine"
      SIDECAR_DATABASE_URL: postgresql+psycopg://scpl_app:scpl_app_dev@postgres:5432/sidecar_pl
      STORE_INTERNAL_URL: http://store-pl:3000
      TRAIT_BASE_URL: http://store-pl:3000
      TRAIT_HMAC_SECRET: conformance-secret-pl
      PAYMENT_PROVIDER: fake
      DEPLOY_PSEUDONYM_KEY: demo-pl-pseudonym-key-32bytes
      OAUTH_CLIENT_ID: demo-agent
      OAUTH_CLIENT_SECRET: demo-agent-secret-pl
      SIDECAR_SIGNING_KEY_PATH: /keys/signing-keys.json
      OPENSTORE_DEMO_MODE: "true"
      OPENSTORE_DEV_PROFILE_HOSTS: buyer-chat:3001
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - plsidecarkeys:/keys
    expose:
      - "8000"
    restart: unless-stopped

  store-pl:
    build:
      context: .
      dockerfile: demo/merchant-site/Dockerfile
    environment:
      PORT: "3000"
      MERCHANT_DATABASE_URL: postgresql://pl_app:pl_app_dev@postgres:5432/pantryline
      TRAIT_HMAC_SECRET: conformance-secret-pl
      SIDECAR_INTERNAL_URL: http://sidecar-pl:8000
      OPENSTORE_PUBLIC_ORIGIN: http://pantryline.localhost
      ADMIN_SEED_PASSWORD: demo-operator-pw
      PUBLIC_SHOP_NAME: "PantryLine"
      PUBLIC_SHOP_TAGLINE: "Pantry staples, sourced and packed."
      PUBLIC_SHOP_BLURB: "a grocer in Ahmedabad"
      PUBLIC_SHOP_ACCENT: "#8a6d2f"
      SEED_PROFILE: pantryline
      PROTOCOL_HEADER: x-forwarded-proto
      NODE_ENV: production
    depends_on:
      postgres:
        condition: service_healthy
    expose:
      - "3000"
    ports:
      - "127.0.0.1:3040:3000"
    restart: unless-stopped

  sidecar-kg:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      OPENSTORE_MERCHANT_DOMAIN: kettleandgrain.localhost
      OPENSTORE_PUBLIC_ORIGIN: http://kettleandgrain.localhost
      WEBAUTHN_RP_ID: kettleandgrain.localhost
      WEBAUTHN_RP_NAME: "Kettle & Grain"
      SIDECAR_DATABASE_URL: postgresql+psycopg://sckg_app:sckg_app_dev@postgres:5432/sidecar_kg
      STORE_INTERNAL_URL: http://store-kg:3000
      TRAIT_BASE_URL: http://store-kg:3000
      TRAIT_HMAC_SECRET: conformance-secret-kg
      PAYMENT_PROVIDER: fake
      DEPLOY_PSEUDONYM_KEY: demo-kg-pseudonym-key-32bytes
      OAUTH_CLIENT_ID: demo-agent
      OAUTH_CLIENT_SECRET: demo-agent-secret-kg
      SIDECAR_SIGNING_KEY_PATH: /keys/signing-keys.json
      OPENSTORE_DEMO_MODE: "true"
      OPENSTORE_DEV_PROFILE_HOSTS: buyer-chat:3001
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - kgsidecarkeys:/keys
    expose:
      - "8000"
    restart: unless-stopped

  store-kg:
    build:
      context: .
      dockerfile: demo/merchant-site/Dockerfile
    environment:
      PORT: "3000"
      MERCHANT_DATABASE_URL: postgresql://kg_app:kg_app_dev@postgres:5432/kettleandgrain
      TRAIT_HMAC_SECRET: conformance-secret-kg
      SIDECAR_INTERNAL_URL: http://sidecar-kg:8000
      OPENSTORE_PUBLIC_ORIGIN: http://kettleandgrain.localhost
      ADMIN_SEED_PASSWORD: demo-operator-pw
      PUBLIC_SHOP_NAME: "Kettle & Grain"
      PUBLIC_SHOP_TAGLINE: "Kitchenware and coffee equipment."
      PUBLIC_SHOP_BLURB: "a kitchenware shop in Jaipur"
      PUBLIC_SHOP_ACCENT: "#8a4a3f"
      SEED_PROFILE: kettleandgrain
      PROTOCOL_HEADER: x-forwarded-proto
      NODE_ENV: production
    depends_on:
      postgres:
        condition: service_healthy
    expose:
      - "3000"
    ports:
      - "127.0.0.1:3050:3000"
    restart: unless-stopped

  sidecar-df:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      OPENSTORE_MERCHANT_DOMAIN: deskfield.localhost
      OPENSTORE_PUBLIC_ORIGIN: http://deskfield.localhost
      WEBAUTHN_RP_ID: deskfield.localhost
      WEBAUTHN_RP_NAME: "DeskField"
      SIDECAR_DATABASE_URL: postgresql+psycopg://scdf_app:scdf_app_dev@postgres:5432/sidecar_df
      STORE_INTERNAL_URL: http://store-df:3000
      TRAIT_BASE_URL: http://store-df:3000
      TRAIT_HMAC_SECRET: conformance-secret-df
      PAYMENT_PROVIDER: fake
      DEPLOY_PSEUDONYM_KEY: demo-df-pseudonym-key-32bytes
      OAUTH_CLIENT_ID: demo-agent
      OAUTH_CLIENT_SECRET: demo-agent-secret-df
      SIDECAR_SIGNING_KEY_PATH: /keys/signing-keys.json
      OPENSTORE_DEMO_MODE: "true"
      OPENSTORE_DEV_PROFILE_HOSTS: buyer-chat:3001
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - dfsidecarkeys:/keys
    expose:
      - "8000"
    restart: unless-stopped

  store-df:
    build:
      context: .
      dockerfile: demo/merchant-site/Dockerfile
    environment:
      PORT: "3000"
      MERCHANT_DATABASE_URL: postgresql://df_app:df_app_dev@postgres:5432/deskfield
      TRAIT_HMAC_SECRET: conformance-secret-df
      SIDECAR_INTERNAL_URL: http://sidecar-df:8000
      OPENSTORE_PUBLIC_ORIGIN: http://deskfield.localhost
      ADMIN_SEED_PASSWORD: demo-operator-pw
      PUBLIC_SHOP_NAME: "DeskField"
      PUBLIC_SHOP_TAGLINE: "Stationery and office supplies."
      PUBLIC_SHOP_BLURB: "a stationer in Delhi"
      PUBLIC_SHOP_ACCENT: "#4a5c8a"
      SEED_PROFILE: deskfield
      PROTOCOL_HEADER: x-forwarded-proto
      NODE_ENV: production
    depends_on:
      postgres:
        condition: service_healthy
    expose:
      - "3000"
    ports:
      - "127.0.0.1:3060:3000"
    restart: unless-stopped

  sidecar-rl:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      OPENSTORE_MERCHANT_DOMAIN: rootandleaf.localhost
      OPENSTORE_PUBLIC_ORIGIN: http://rootandleaf.localhost
      WEBAUTHN_RP_ID: rootandleaf.localhost
      WEBAUTHN_RP_NAME: "Root & Leaf"
      SIDECAR_DATABASE_URL: postgresql+psycopg://scrl_app:scrl_app_dev@postgres:5432/sidecar_rl
      STORE_INTERNAL_URL: http://store-rl:3000
      TRAIT_BASE_URL: http://store-rl:3000
      TRAIT_HMAC_SECRET: conformance-secret-rl
      PAYMENT_PROVIDER: fake
      DEPLOY_PSEUDONYM_KEY: demo-rl-pseudonym-key-32bytes
      OAUTH_CLIENT_ID: demo-agent
      OAUTH_CLIENT_SECRET: demo-agent-secret-rl
      SIDECAR_SIGNING_KEY_PATH: /keys/signing-keys.json
      OPENSTORE_DEMO_MODE: "true"
      OPENSTORE_DEV_PROFILE_HOSTS: buyer-chat:3001
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - rlsidecarkeys:/keys
    expose:
      - "8000"
    restart: unless-stopped

  store-rl:
    build:
      context: .
      dockerfile: demo/merchant-site/Dockerfile
    environment:
      PORT: "3000"
      MERCHANT_DATABASE_URL: postgresql://rl_app:rl_app_dev@postgres:5432/rootandleaf
      TRAIT_HMAC_SECRET: conformance-secret-rl
      SIDECAR_INTERNAL_URL: http://sidecar-rl:8000
      OPENSTORE_PUBLIC_ORIGIN: http://rootandleaf.localhost
      ADMIN_SEED_PASSWORD: demo-operator-pw
      PUBLIC_SHOP_NAME: "Root & Leaf"
      PUBLIC_SHOP_TAGLINE: "Potted plants and garden supplies."
      PUBLIC_SHOP_BLURB: "a nursery in Kochi"
      PUBLIC_SHOP_ACCENT: "#4a8a5c"
      SEED_PROFILE: rootandleaf
      PROTOCOL_HEADER: x-forwarded-proto
      NODE_ENV: production
    depends_on:
      postgres:
        condition: service_healthy
    expose:
      - "3000"
    ports:
      - "127.0.0.1:3070:3000"
    restart: unless-stopped

  sidecar-ps:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      OPENSTORE_MERCHANT_DOMAIN: playspool.localhost
      OPENSTORE_PUBLIC_ORIGIN: http://playspool.localhost
      WEBAUTHN_RP_ID: playspool.localhost
      WEBAUTHN_RP_NAME: "Playspool"
      SIDECAR_DATABASE_URL: postgresql+psycopg://scps_app:scps_app_dev@postgres:5432/sidecar_ps
      STORE_INTERNAL_URL: http://store-ps:3000
      TRAIT_BASE_URL: http://store-ps:3000
      TRAIT_HMAC_SECRET: conformance-secret-ps
      PAYMENT_PROVIDER: fake
      DEPLOY_PSEUDONYM_KEY: demo-ps-pseudonym-key-32bytes
      OAUTH_CLIENT_ID: demo-agent
      OAUTH_CLIENT_SECRET: demo-agent-secret-ps
      SIDECAR_SIGNING_KEY_PATH: /keys/signing-keys.json
      OPENSTORE_DEMO_MODE: "true"
      OPENSTORE_DEV_PROFILE_HOSTS: buyer-chat:3001
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - pssidecarkeys:/keys
    expose:
      - "8000"
    restart: unless-stopped

  store-ps:
    build:
      context: .
      dockerfile: demo/merchant-site/Dockerfile
    environment:
      PORT: "3000"
      MERCHANT_DATABASE_URL: postgresql://ps_app:ps_app_dev@postgres:5432/playspool
      TRAIT_HMAC_SECRET: conformance-secret-ps
      SIDECAR_INTERNAL_URL: http://sidecar-ps:8000
      OPENSTORE_PUBLIC_ORIGIN: http://playspool.localhost
      ADMIN_SEED_PASSWORD: demo-operator-pw
      PUBLIC_SHOP_NAME: "Playspool"
      PUBLIC_SHOP_TAGLINE: "Toys and games for every age."
      PUBLIC_SHOP_BLURB: "a toyshop in Ludhiana"
      PUBLIC_SHOP_ACCENT: "#8a3f6b"
      SEED_PROFILE: playspool
      PROTOCOL_HEADER: x-forwarded-proto
      NODE_ENV: production
    depends_on:
      postgres:
        condition: service_healthy
    expose:
      - "3000"
    ports:
      - "127.0.0.1:3080:3000"
    restart: unless-stopped

  sidecar-fw:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      OPENSTORE_MERCHANT_DOMAIN: furrow.localhost
      OPENSTORE_PUBLIC_ORIGIN: http://furrow.localhost
      WEBAUTHN_RP_ID: furrow.localhost
      WEBAUTHN_RP_NAME: "Furrow"
      SIDECAR_DATABASE_URL: postgresql+psycopg://scfw_app:scfw_app_dev@postgres:5432/sidecar_fw
      STORE_INTERNAL_URL: http://store-fw:3000
      TRAIT_BASE_URL: http://store-fw:3000
      TRAIT_HMAC_SECRET: conformance-secret-fw
      PAYMENT_PROVIDER: fake
      DEPLOY_PSEUDONYM_KEY: demo-fw-pseudonym-key-32bytes
      OAUTH_CLIENT_ID: demo-agent
      OAUTH_CLIENT_SECRET: demo-agent-secret-fw
      SIDECAR_SIGNING_KEY_PATH: /keys/signing-keys.json
      OPENSTORE_DEMO_MODE: "true"
      OPENSTORE_DEV_PROFILE_HOSTS: buyer-chat:3001
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - fwsidecarkeys:/keys
    expose:
      - "8000"
    restart: unless-stopped

  store-fw:
    build:
      context: .
      dockerfile: demo/merchant-site/Dockerfile
    environment:
      PORT: "3000"
      MERCHANT_DATABASE_URL: postgresql://fw_app:fw_app_dev@postgres:5432/furrow
      TRAIT_HMAC_SECRET: conformance-secret-fw
      SIDECAR_INTERNAL_URL: http://sidecar-fw:8000
      OPENSTORE_PUBLIC_ORIGIN: http://furrow.localhost
      ADMIN_SEED_PASSWORD: demo-operator-pw
      PUBLIC_SHOP_NAME: "Furrow"
      PUBLIC_SHOP_TAGLINE: "Everyday pet supplies."
      PUBLIC_SHOP_BLURB: "a pet shop in Lucknow"
      PUBLIC_SHOP_ACCENT: "#6b8a3f"
      SEED_PROFILE: furrow
      PROTOCOL_HEADER: x-forwarded-proto
      NODE_ENV: production
    depends_on:
      postgres:
        condition: service_healthy
    expose:
      - "3000"
    ports:
      - "127.0.0.1:3090:3000"
    restart: unless-stopped

  buyer-chat:
    build:
      context: .
      dockerfile: demo/buyer-chat/Dockerfile
    # Provider keys live in `.env.llm`, which Compose does not read for
    # interpolation the way it reads `.env` — so the file is mounted as an
    # env_file instead. Both are optional: a machine with neither still boots,
    # and the driver below still defaults to the pinned planner.
    #
    # An `environment:` entry would WIN over these, so OPENROUTER_API_KEY is
    # deliberately absent from the block below: naming it there as
    # `${OPENROUTER_API_KEY:-}` resolves to the empty string when the shell has
    # not exported it, and quietly overwrites the key this file just supplied.
    env_file:
      - path: .env.llm
        required: false
      - path: .env
        required: false
    environment:
      PORT: "3001"
      # A real model when a key is present, the pinned planner otherwise. The
      # demo must still run end to end with no key and no network to a model —
      # a demo that needs somebody's API key to pass its own tests is one that
      # fails on the next laptop.
      CHAT_MODEL_DRIVER: ${CHAT_MODEL_DRIVER:-scripted}
      OPENROUTER_MODEL: ${OPENROUTER_MODEL:-openai/gpt-oss-20b}
      CHAT_DATABASE_PATH: /data/chat.db
      CHAT_KEY_PATH: /data/agent-key.json
      # The chat reaches the sidecar through Caddy carrying the real Host
      # header: a card fetched as http://caddy/ would advertise the wrong
      # origin back to the browser (§10.1).
      SIDECAR_PUBLIC_ORIGIN: http://spoiledduckie.localhost
      # The chat dials the shop by its own domain (the edge answers to it on
      # this network), and names it as the one host it may reach over plain
      # http — the mirror of the sidecar's own exception.
      OPENSTORE_DEV_PROFILE_HOSTS: spoiledduckie.localhost,dogeared.localhost,circuityard.localhost,ironlist.localhost,pantryline.localhost,kettleandgrain.localhost,deskfield.localhost,rootandleaf.localhost,playspool.localhost,furrow.localhost
      # adapter-node assumes https when no protocol header is named, so the
      # origin it computes is https://<host> while the browser posts from
      # http://<host> — and SvelteKit's CSRF check refuses every form on the
      # demo's plain-http origins. Caddy sets this header; honour it.
      PROTOCOL_HEADER: x-forwarded-proto
      NODE_ENV: production
    volumes:
      - chatdata:/data
    expose:
      - "3001"
    restart: unless-stopped

  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_PASSWORD: postgres_dev
    ports:
      # Published for local development and the seed script. The compose network
      # is how the services actually reach it; this is for a human with psql.
      - "127.0.0.1:5432:5432"
    volumes:
      - ./docker/postgres-init.sql:/docker-entrypoint-initdb.d/00-init.sql:ro
      - pgdata:/var/lib/postgresql/data
    expose:
      - "5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 3s
      retries: 10
    restart: unless-stopped

volumes:
  pgdata:
  chatdata:
  sidecarkeys:
  booksidecarkeys:
  cysidecarkeys:
  ilsidecarkeys:
  plsidecarkeys:
  kgsidecarkeys:
  dfsidecarkeys:
  rlsidecarkeys:
  pssidecarkeys:
  fwsidecarkeys:
